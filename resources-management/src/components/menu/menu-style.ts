import styled from "styled-components";

export const Navigation = styled.section`
    position: fixed;
    top: 0;

    width: calc(max(100vw, 329px));
    height: 40px;
    background: black;
    color: white;

    display: flex;
    align-items: center;
    

    div{
        width: 90px;
        height: 20px;
        margin: 10px;
        text-align: center;
    }

`